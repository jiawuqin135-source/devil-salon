# 恶魔理发师 · 小红书小工具 ZIP 校验摘要

## 采用规范
已按用户指定链接下载 minitool-zip-builder 1.6.0，解压位置：
/Users/lele/Documents/Codex/2026-09-22/ban/.codex/minitool-zip-builder/
已读取 SKILL.md 及 ZIP、端能力、JS、CSS、跨端和性能规范。未使用 JSBridge，因此 Native API 项不适用。

## 交付物
上传包：恶魔理发师-小红书小工具上传包.zip
解压产物目录：恶魔理发师-小工具H5/
文件数：10；ZIP 字节数：1449682（1.38 MiB）。
SHA-256：06b40db47aca006a4d39407fabbff048e80e4ecccf6f5a9060a56a44cbe17500

本包是离线 H5 小工具上传产物，入口 index.html 直接位于 ZIP 根目录。此前“双端工程包”包含工程外层目录，不能替代本上传包。

## 已修复
- 去除 Service Worker、主屏幕安装及相关配置、WebMCP 接口。
- 去除 fetch 音效加载。规范的文件后缀白名单未列出 WAV，故未将 WAV 塞入包内；改为 Web Audio 即时合成短音效，用户点击后启用，不支持时禁用音效按钮。
- 去除可选链、对象展开、无参数 catch、replaceChildren 等超出基线写法，保留经典脚本与 ES2017 语法。
- 用百分比比例盒、物理定位、margin/grid-gap、vh、rgba 提供 Chrome 61 CSS 基线；aspect-ratio、clamp、背景模糊仅通过 @supports 增强。
- 安全区支持容器注入变量及 env 回退；页面隐藏时暂停游戏、停止声音及动画循环。
- 保留 12 关、顺序解锁、难度递增、角色/顾客图集、理发店背景和下移后的气泡。

## 校验结果
- Skill 自带 Python 审计：产物目录 PASS，10 个文件、0 警告；最终 ZIP PASS，10 个文件、0 警告。
- 补充静态检查：允许文件后缀、根入口、所有资源路径、经典脚本顺序、禁止 API、无外部 URL、无内联脚本/事件、无自建 CSP、JS 语法检查通过。
- 无 Base64、无内置大型数据集、无 WebGL；相关资源/降级条目不适用。
- 492 次模拟通关通过，检查失败不解锁、进度存档、暂停、超时、难度参数。
- 现代浏览器冒烟检查：首屏正常、开始倒计时正常、不剪发仍耗时、暂停弹层正常。

## 实测边界
Chrome 61 / Android 8.1 JS 与 CSS 兼容性未实测；小红书官方模拟器和 iPhone WebView 未实测。音效听感、真机首屏时间、帧率、内存和低端设备性能未实测。本报告的静态通过不等于平台审核通过。

本次新增插画封面、天使与恶魔的玩法对白，以及「开始游戏」按钮；上传包已更新，未提交小红书审核。

首页更新：采用用户确认的全屏封面，对白融入角色气泡；插画按钮位置叠加可键盘操作的真实开始按钮。
